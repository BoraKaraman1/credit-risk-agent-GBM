[pytest]
# Collect only the maintained suite. backup_python/ holds the retired
# Python services (kept for reference) whose tests assume backup_python/
# as the import root; collecting them from the repo root fails. CI already
# runs `pytest tests/`; this makes a bare `pytest` behave the same.
testpaths = tests
